

const Detail = ({detail}) => {
    const {_id, photo, name, quantity, author, category, description, rating} = detail;
    return (
        <div>
            
        </div>
    );
};

export default Detail;